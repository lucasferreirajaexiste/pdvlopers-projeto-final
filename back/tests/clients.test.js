// Testes para clientes (Back 2 - Fabio N.)
const _request = require("supertest");
const _app = require("../src/server");

describe("Client Routes", () => {
  test("GET /api/clients should return clients list", async () => {
    // Implementar teste
    expect(true).toBe(true);
  });

  test("POST /api/clients should create a new client", async () => {
    // Implementar teste
    expect(true).toBe(true);
  });
});
